name: example
description: Example app for form_location_field package
homepage: https://github.com/anycode/form_location_field/tree/master/example
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  location_form_field:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
