name: location_form_field
description: Location Input Field for Flutter Forms. Used to select coodinates on a map.
version: 1.0.0-nullsafety.01
homepage: https://github.com/anycode/form_location_field

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
